version: "3.8"
services:
  bot:
    build: .
    ports:
      - "8000:8000"   # FastAPI
      - "8501:8501"   # Streamlit
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
